import { createApp } from 'vue';
import App from './App.vue';
import AdminDashboard from './components/Admin/Dashboard.vue';

createApp(App).mount('#app');
createApp(AdminDashboard).mount('#admin-dashboard-vue');
